# compiled output
/dist
/node_modules
/build

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# Env - ignore every real env file, keep the committed template
.env
.env.*
!.env.example

# Generated by scripts/setup-ec2.sh at deploy time -- livekit.yaml embeds the
# raw LIVEKIT_API_SECRET in plaintext and must never be committed. Both files
# are fully regenerated by the script on every run, so there is nothing here
# worth keeping in git anyway.
/docker-compose.yml
/livekit.yaml

# Misc
*.tsbuildinfo
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat
.gitignore
.claude/
